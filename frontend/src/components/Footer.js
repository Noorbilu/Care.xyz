export default function Footer() {
  return (
    <footer className="border-t py-4 mt-8 text-center text-sm text-gray-500">
      © {new Date().getFullYear()} Care.IO — Making caregiving easy, secure & accessible.
    </footer>
  );
}